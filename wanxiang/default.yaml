# @format

# Rime default settings
# encoding: utf-8

# 要比共享目录的同名文件的 config_version 大才可以生效
config_version: "LTS"

schema_list:
  - schema: wanxiang_flypy
  - schema: wanxiang_flypy_18keys
  - schema: wanxiang_flypy_14keys
  - schema: wanxiang_l17keys
  - schema: wanxiang_t9
  - schema: wanxiang_english

menu:
  page_size: 5
  alternative_select_labels: [〡, 〢, 〣, 〤, 〥, 〦, 〧, 〨, 〩, 〸]

switcher:
  caption: 「状态面板」
  hotkeys:
    - Control+grave
  save_options:
    - ascii_punct
    - emoji
    - full_shape
    - prediction
    - chaifen_tips
    - tone_display
    - aux_hint
    - tone_hint
    - full_pinyin
    - chinese_english
    - abbrev
  fold_options: true
  abbreviate_options: true
  option_list_separator: " / "

ascii_composer:
  good_old_caps_lock: true
  switch_key:
    Caps_Lock: clear
    Shift_L: commit_code
    Shift_R: commit_code
    Control_L: noop
    Control_R: noop

key_binder:
  bindings:
    - { when: always, accept: Alt+Shift+n, select: .next }
    - { accept: KP_0, send: 0, when: composing }
    - { accept: KP_1, send: 1, when: composing }
    - { accept: KP_2, send: 2, when: composing }
    - { accept: KP_3, send: 3, when: composing }
    - { accept: KP_4, send: 4, when: composing }
    - { accept: KP_5, send: 5, when: composing }
    - { accept: KP_6, send: 6, when: composing }
    - { accept: KP_7, send: 7, when: composing }
    - { accept: KP_8, send: 8, when: composing }
    - { accept: KP_9, send: 9, when: composing }
    - { accept: KP_Decimal, send: period, when: composing }
    - { accept: KP_Multiply, send: asterisk, when: composing }
    - { accept: KP_Add, send: plus, when: composing }
    - { accept: KP_Subtract, send: minus, when: composing }
    - { accept: KP_Divide, send: slash, when: composing }
    - { accept: KP_Enter, send: Return, when: composing }
